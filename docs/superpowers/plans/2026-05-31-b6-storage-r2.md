# Storage R2 (B6) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Câbler le stockage de fichiers sur Cloudflare R2 (bucket unique + préfixes) et porter les sous-routes fichier reportées (avatar, prescription document, document file, payslips).

**Architecture:** `StorageModule` global + `StorageService` (S3Client R2, bucket unique, upload/getStream/delete + key builders) ; sous-routes fichier ajoutées aux modules existants via `FileInterceptor` (multer) ; download = streaming via backend. Creds R2 optionnels (503 si absents).

**Tech Stack:** NestJS, @aws-sdk/client-s3, @nestjs/platform-express (multer), Drizzle, Vitest.

> ⚠️ J-Ned : commits locaux, **jamais de push**. Cwd : `/home/jned/WebstormProjects/DashFlow/nest-dashflow-app/`.
> **Source de port :** `dash-flow/backend/src/storage/s3.ts` + les sous-routes fichier de `dash-flow/backend/src/routes/{auth,prescription,document,recurring-entry,salary-archive}.routes.ts`.
> Réutilise : `OwnedCrudService`/services existants, `JwtAuthGuard`, `CsrfGuard`, `@CurrentUser`, `parseBody`. Schéma : colonnes `avatarUrl` (users), `documentUrl` (prescriptions), `fileUrl` (documents), `payslipKey` (recurring_entries, salary_archives).

---

## File Structure

| Fichier | Rôle |
|---|---|
| `src/config/env.schema.ts` | + S3_ENDPOINT?, S3_REGION (auto), S3_ACCESS_KEY_ID?, S3_SECRET_ACCESS_KEY?, S3_BUCKET? |
| `src/storage/storage.service.ts` | S3Client R2 + upload/getStream/delete + key builders |
| `src/storage/storage.module.ts` | `@Global` fournit StorageService |
| `src/storage/storage.service.spec.ts` | unit (S3 mocké) |
| `src/auth/auth.controller.ts` | + `POST /me/avatar`, `GET /avatar/:userId` |
| `src/auth/auth.module.ts` | (StorageModule global — rien à importer) |
| `src/modules/prescriptions/*` | + `GET/POST/DELETE /:id/document` |
| `src/modules/documents/*` | + `GET/POST/DELETE /:id/file` |
| `src/modules/recurring-entries/*` | + `GET/POST/DELETE /:id/payslip` |
| `src/modules/salary-archives/*` | + `GET/POST/DELETE /:id/payslip` |
| `src/app.module.ts` | + StorageModule |
| `.env` / `.env.example` | vars R2 (valeurs fournies par l'utilisateur dans `.env`) |

---

## Task 1: Deps + env + StorageModule/Service

**Files:** `package.json`, `src/config/env.schema.ts`, `.env.example`, `src/storage/storage.service.ts`(+spec), `src/storage/storage.module.ts`, `src/app.module.ts`

- [ ] **Step 1:** `pnpm add @aws-sdk/client-s3@^3.1004.0`

- [ ] **Step 2:** Ajouter à `env.schema.ts` :
```ts
  S3_ENDPOINT: z.string().url().optional(),
  S3_REGION: z.string().default('auto'),
  S3_ACCESS_KEY_ID: z.string().optional(),
  S3_SECRET_ACCESS_KEY: z.string().optional(),
  S3_BUCKET: z.string().optional(),
```
Ajouter à `.env.example` (et l'utilisateur remplira `.env`) :
```dotenv
S3_ENDPOINT=
S3_REGION=auto
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=
```
(Laisser commentées dans `.env` tant que vides pour rester `undefined`.)

- [ ] **Step 3: Test** `src/storage/storage.service.spec.ts` — injecter un faux S3 client. Le service expose `send` via une méthode interne ; pour testabilité, le constructeur prend la config et construit le client, mais on teste les **key builders** (purs) + le comportement "non configuré". Test :
```ts
import { describe, it, expect } from 'vitest';
import { StorageService } from './storage.service';

const cfg = (configured: boolean) => ({ get: (k: string) => configured
  ? ({ S3_ENDPOINT:'https://x.r2.cloudflarestorage.com', S3_REGION:'auto', S3_ACCESS_KEY_ID:'a', S3_SECRET_ACCESS_KEY:'b', S3_BUCKET:'dashflow-app' } as any)[k]
  : ({ S3_REGION:'auto' } as any)[k] });

describe('StorageService key builders', () => {
  const svc = new StorageService(cfg(true) as any);
  it('avatarKey', () => { expect(svc.avatarKey('u1','image/png')).toBe('avatars/u1.png'); });
  it('avatarKey jpeg→jpg', () => { expect(svc.avatarKey('u1','image/jpeg')).toBe('avatars/u1.jpg'); });
  it('prescriptionKey', () => { expect(svc.prescriptionKey('u1','p1','application/pdf')).toBe('prescriptions/u1/p1.pdf'); });
  it('payslipKey', () => { expect(svc.payslipKey('u1','e1','application/pdf')).toBe('payslips/u1/e1.pdf'); });
});

describe('StorageService non configuré', () => {
  it('upload lance 503 si pas de client', async () => {
    const svc = new StorageService(cfg(false) as any);
    await expect(svc.upload('k', Buffer.from('x'), 'text/plain')).rejects.toThrow();
  });
});
```

- [ ] **Step 4:** Implémenter `src/storage/storage.service.ts` :
```ts
import { Injectable, ServiceUnavailableException } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { S3Client, PutObjectCommand, GetObjectCommand, DeleteObjectCommand } from '@aws-sdk/client-s3';
import type { Readable } from 'node:stream';
import type { Env } from '../config/env.schema';

function ext(contentType: string, fallback: string): string {
  return (contentType.split('/')[1] ?? fallback).replace('jpeg', 'jpg');
}

@Injectable()
export class StorageService {
  private readonly client: S3Client | null;
  private readonly bucket: string | undefined;

  constructor(config: ConfigService<Env, true>) {
    const endpoint = config.get('S3_ENDPOINT', { infer: true });
    const accessKeyId = config.get('S3_ACCESS_KEY_ID', { infer: true });
    const secretAccessKey = config.get('S3_SECRET_ACCESS_KEY', { infer: true });
    this.bucket = config.get('S3_BUCKET', { infer: true });
    this.client = endpoint && accessKeyId && secretAccessKey && this.bucket
      ? new S3Client({ region: config.get('S3_REGION', { infer: true }), endpoint, credentials: { accessKeyId, secretAccessKey }, forcePathStyle: true })
      : null;
  }

  private ready(): { client: S3Client; bucket: string } {
    if (!this.client || !this.bucket) throw new ServiceUnavailableException('Stockage R2 non configuré');
    return { client: this.client, bucket: this.bucket };
  }

  avatarKey(userId: string, ct: string): string { return `avatars/${userId}.${ext(ct, 'jpg')}`; }
  prescriptionKey(userId: string, id: string, ct: string): string { return `prescriptions/${userId}/${id}.${ext(ct, 'pdf')}`; }
  documentKey(userId: string, id: string, ct: string): string { return `documents/${userId}/${id}.${ext(ct, 'pdf')}`; }
  payslipKey(userId: string, id: string, ct: string): string { return `payslips/${userId}/${id}.${ext(ct, 'pdf')}`; }

  async upload(key: string, body: Buffer, contentType: string, cacheControl = 'private, max-age=86400'): Promise<void> {
    const { client, bucket } = this.ready();
    await client.send(new PutObjectCommand({ Bucket: bucket, Key: key, Body: body, ContentType: contentType, CacheControl: cacheControl }));
  }

  async getStream(key: string): Promise<{ stream: Readable; contentType: string } | null> {
    const { client, bucket } = this.ready();
    try {
      const res = await client.send(new GetObjectCommand({ Bucket: bucket, Key: key }));
      if (!res.Body) return null;
      return { stream: res.Body as Readable, contentType: res.ContentType ?? 'application/octet-stream' };
    } catch { return null; }
  }

  async delete(key: string): Promise<void> {
    if (!this.client || !this.bucket) return;
    try { await this.client.send(new DeleteObjectCommand({ Bucket: this.bucket, Key: key })); } catch { /* non-critique */ }
  }
}
```

- [ ] **Step 5:** `src/storage/storage.module.ts` :
```ts
import { Global, Module } from '@nestjs/common';
import { StorageService } from './storage.service';
@Global()
@Module({ providers: [StorageService], exports: [StorageService] })
export class StorageModule {}
```
Ajouter `StorageModule` aux imports de `src/app.module.ts`.

- [ ] **Step 6:** `pnpm test` + `pnpm tsc --noEmit` verts. **Step 7: Commit** — `feat(b6): StorageModule + StorageService R2 (bucket unique)`

---

## Task 2: avatar (auth) — template fichier

**Files:** Modify `src/auth/auth.controller.ts`. **Source : routes avatar de `auth.routes.ts`.**

- [ ] **Step 1:** Ajouter à `AuthController` (avatar GET PUBLIC = pas de guard ; POST = Jwt+Csrf + FileInterceptor). Imports : `UploadedFile, UseInterceptors` de `@nestjs/common`, `FileInterceptor` de `@nestjs/platform-express`, `StorageService`, `Response` d'express.
```ts
  // POST /api/auth/me/avatar
  @UseGuards(JwtAuthGuard, CsrfGuard)
  @Post('me/avatar')
  @UseInterceptors(FileInterceptor('avatar', { limits: { fileSize: 2 * 1024 * 1024 } }))
  async uploadAvatar(@CurrentUser() u: AuthUser, @UploadedFile() file: { buffer: Buffer; mimetype: string } | undefined) {
    if (!file) throw new BadRequestException('Fichier requis');
    if (!['image/jpeg', 'image/png', 'image/webp', 'image/gif'].includes(file.mimetype)) throw new BadRequestException('Type image invalide');
    const key = this.storage.avatarKey(u.id, file.mimetype);
    await this.storage.upload(key, file.buffer, file.mimetype, 'public, max-age=31536000, immutable');
    await this.auth.setAvatar(u.id, key);   // ajoute setAvatar(userId, key) dans AuthService → updateUser({ avatarUrl: key })
    return { avatarUrl: `/api/auth/avatar/${u.id}` };
  }

  // GET /api/auth/avatar/:userId (PUBLIC)
  @Get('avatar/:userId')
  async getAvatar(@Param('userId') userId: string, @Res() res: Response): Promise<void> {
    const user = await this.auth.getById(userId);
    if (!user?.avatarUrl) throw new NotFoundException('Avatar introuvable');
    const obj = await this.storage.getStream(user.avatarUrl);
    if (!obj) throw new NotFoundException('Avatar introuvable');
    res.setHeader('Content-Type', obj.contentType);
    obj.stream.pipe(res);
  }
```
Injecter `StorageService` dans le constructeur d'`AuthController`. Ajouter `setAvatar(userId, key)` à `AuthService` (`updateUser(userId, { avatarUrl: key })`). Importer `BadRequestException`, `NotFoundException`, `Param`, `Get`, `Res`.
> Note : `avatarUrl` stocke la **clé** R2 (ex. `avatars/<id>.png`) ; `toPublicUser` renvoie déjà `/api/auth/avatar/<id>` quand `avatarUrl` est non-null — cohérent.

- [ ] **Step 2:** `pnpm tsc --noEmit` + `pnpm test`. Boot smoke : routes `POST /api/auth/me/avatar` + `GET /api/auth/avatar/:userId` mappées ; tuer serveur + libérer :3001.

- [ ] **Step 3: Commit** — `feat(b6): avatar upload/stream (R2)`

---

## Task 3: prescriptions /:id/document + documents /:id/file

**Files:** Modify `src/modules/prescriptions/*`, `src/modules/documents/*`. **Sources : `prescription.routes.ts`, `document.routes.ts` (sous-routes fichier).**

- [ ] **Step 1: prescriptions** — injecter `StorageService` dans `PrescriptionsService` (ou le controller). Ajouter au controller (suivre le template avatar ; ownership via `getOne(u.id, id)` AVANT accès R2) :
  - `@UseGuards(JwtAuthGuard, CsrfGuard) @Post(':id/document')` + `FileInterceptor('document', { limits:{fileSize:10*1024*1024} })` → vérifier ownership (`getOne`, 404), `key = storage.prescriptionKey(u.id, id, mimetype)`, `storage.upload(key, buffer, mimetype)`, `update(u.id, id, { documentUrl: key })`, renvoyer la ressource.
  - `@UseGuards(JwtAuthGuard) @Get(':id/document')` → ownership, lire `documentUrl`, `getStream` → pipe (404 si absent). **Déclarer avant `@Get(':id')`** dans la classe (sinon `:id` capture). En réalité `:id/document` ne collisionne pas avec `:id` (segment supplémentaire) mais garder l'ordre logique.
  - `@UseGuards(JwtAuthGuard, CsrfGuard) @Delete(':id/document')` → ownership, `storage.delete(key)`, `update(u.id, id, { documentUrl: null })`, 204.
- [ ] **Step 2: documents** — même chose avec `fileUrl` + `storage.documentKey` + interceptor `FileInterceptor('file')` + routes `/:id/file`.
- [ ] **Step 3:** `pnpm tsc --noEmit` + `pnpm test` verts.
- [ ] **Step 4: Commit** — `feat(b6): prescriptions document + documents file (R2)`

---

## Task 4: recurring-entries + salary-archives payslip

**Files:** Modify `src/modules/recurring-entries/*`, `src/modules/salary-archives/*`. **Sources : `recurring-entry.routes.ts`, `salary-archive.routes.ts` (sous-routes payslip).**

- [ ] **Step 1:** Pour chaque module, injecter `StorageService` et ajouter `GET/POST/DELETE /:id/payslip` (même template : ownership via `getOne`, `key = storage.payslipKey(u.id, id, mimetype)`, upload→`update({ payslipKey: key })`, get→stream, delete→`storage.delete` + `update({ payslipKey: null })`). Interceptor `FileInterceptor('payslip', { limits:{fileSize:10*1024*1024} })`. Port fidèle des routes Hono correspondantes.
- [ ] **Step 2:** `pnpm tsc --noEmit` + `pnpm test`. Boot smoke : routes payslip mappées ; tuer serveur + libérer :3001.
- [ ] **Step 3: Commit** — `feat(b6): recurring-entries + salary-archives payslip (R2)`

---

## Task 5: Test live R2 (GATE — nécessite les creds utilisateur)

**Files:** aucun (vérification manuelle) — ou `test/storage.e2e-spec.ts` si creds en CI.

> ⚠️ Exécuté par le contrôleur une fois que l'utilisateur a fourni les creds R2 dans `.env`. Sinon, les unit tests (S3 mocké) couvrent la logique et cette étape est marquée "en attente creds".

- [ ] **Step 1:** Vérifier `.env` peuplé (S3_ENDPOINT/KEY/SECRET/BUCKET). Démarrer l'app.
- [ ] **Step 2:** Flow manuel (authentifié, avec cookie+csrf) : `POST /api/auth/me/avatar` (multipart image) → 200 ; `GET /api/auth/avatar/:userId` → 200 + image ; `DELETE`/re-`GET` → 404. Confirmer l'objet présent dans le bucket R2 (console CF).
- [ ] **Step 3:** Marquer B6 complet.

---

## Self-Review

**Couverture du spec :**
- StorageModule/Service R2 bucket unique + key builders + 503 → Task 1 ✓
- Streaming via backend + ownership → Tasks 2-4 ✓
- avatar (public GET) → Task 2 ✓
- prescriptions/documents/recurring/salary file routes (parité Hono) → Tasks 3-4 ✓
- env optionnels + boot sans creds → Task 1 ✓
- live R2 (gate creds) → Task 5 ✓

**Placeholders :** StorageService + avatar inlinés (template) ; routes par module décrites par contrat + template avatar + source Hono. `setAvatar` ajouté à AuthService (référencé Task 2). Pas de TODO.

**Cohérence des types :** `StorageService` (Task 1) injecté Tasks 2-4 ; key builders cohérents ; colonnes `avatarUrl`/`documentUrl`/`fileUrl`/`payslipKey` (schéma B0) ; `getStream` retourne `{stream, contentType}` piped partout pareil ; FileInterceptor field names (`avatar`/`document`/`file`/`payslip`) cohérents avec les `@UploadedFile`.
