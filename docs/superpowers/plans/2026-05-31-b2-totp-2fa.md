# TOTP 2FA (B2) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter la 2FA TOTP (setup/verify/disable + gate au login) au module auth NestJS, port fidèle de Hono.

**Architecture:** `TwoFactorService` (otpauth+qrcode) injecté dans `AuthService` (setup/enable/disable) et le gate dans `AuthService.login`. Endpoints sous `/auth/me/2fa/*` (JwtAuthGuard+CsrfGuard). `Result.fail` étendu d'un `code?` pour `TOTP_REQUIRED`.

**Tech Stack:** NestJS, otpauth, qrcode, argon2, Zod, Vitest.

> ⚠️ J-Ned : commits locaux autorisés, **jamais de push**. Cwd : `/home/jned/WebstormProjects/DashFlow/nest-dashflow-app/`. DB locale up pour l'e2e.
> Contexte existant (B1) : `AuthService` (repo + mailer), `AuthRepository` (`findById`, `updateUser`), `auth.controller.ts` (guards, `httpFrom`, `ZodValidationPipe`), `auth.result.ts` (`ok`/`fail`/`Result`), `loginSchema`/`LoginDto` dans `dto/auth.dto.ts`, `auth.module.ts`.

---

## File Structure

| Fichier | Action |
|---|---|
| `package.json` | + otpauth, qrcode, @types/qrcode |
| `src/auth/auth.result.ts` | `fail` gagne un `code?` optionnel |
| `src/auth/auth.controller.ts` | `httpFrom` propage `code` ; 3 endpoints 2FA |
| `src/auth/two-factor.service.ts` | nouveau service otpauth/qrcode |
| `src/auth/auth.service.ts` | `setupTotp`/`enableTotp`/`disableTotp` + gate `login` |
| `src/auth/dto/auth.dto.ts` | `totpVerifySchema`, `totpDisableSchema`, `loginSchema` += `totpCode?` |
| `src/auth/auth.module.ts` | provider `TwoFactorService` |
| `test/auth.e2e-spec.ts` | scénario 2FA |

---

## Task 1: Deps + Result.code

**Files:** Modify `package.json`, `src/auth/auth.result.ts`, `src/auth/auth.controller.ts`

- [ ] **Step 1:** `pnpm add otpauth@^9.5.0 qrcode@^1.5.4 && pnpm add -D @types/qrcode@^1.5.6`

- [ ] **Step 2:** Étendre `src/auth/auth.result.ts` — remplacer le type et `fail` :
```ts
export type Result<T> = { success: true; data: T } | { success: false; error: string; status: number; code?: string };
export const ok = <T>(data: T): Result<T> => ({ success: true, data });
export const fail = (status: number, error: string, code?: string): Result<never> => ({ success: false, error, status, code });
```

- [ ] **Step 3:** Dans `src/auth/auth.controller.ts`, remplacer `httpFrom` pour propager le `code` :
```ts
function httpFrom(r: { status: number; error: string; code?: string }): HttpException {
  return new HttpException(r.code ? { message: r.error, code: r.code } : r.error, r.status);
}
```

- [ ] **Step 4:** `pnpm test` (rien cassé) + `pnpm tsc --noEmit` clean.

- [ ] **Step 5: Commit** — `feat(b2): deps otpauth/qrcode + Result.code pour TOTP_REQUIRED`

---

## Task 2: TwoFactorService

**Files:** Create `src/auth/two-factor.service.ts`; Test `src/auth/two-factor.service.spec.ts`

- [ ] **Step 1: Test (TDD)** `src/auth/two-factor.service.spec.ts` :
```ts
import { describe, it, expect } from 'vitest';
import * as OTPAuth from 'otpauth';
import { TwoFactorService } from './two-factor.service';

describe('TwoFactorService', () => {
  const svc = new TwoFactorService();

  it('génère un secret base32 + URI otpauth', () => {
    const { secret, otpauthUri } = svc.generateSecret('a@b.com');
    expect(secret).toMatch(/^[A-Z2-7]+$/);
    expect(otpauthUri).toContain('otpauth://totp/');
  });

  it('verify accepte un code calculé depuis le secret, rejette un faux', () => {
    const { secret } = svc.generateSecret('a@b.com');
    const totp = new OTPAuth.TOTP({ issuer: 'DashFlow', label: 'a@b.com', secret: OTPAuth.Secret.fromBase32(secret) });
    expect(svc.verify(secret, totp.generate())).toBe(true);
    expect(svc.verify(secret, '000000')).toBe(false);
  });
});
```

- [ ] **Step 2:** Run → FAIL.

- [ ] **Step 3:** `src/auth/two-factor.service.ts` :
```ts
import { Injectable } from '@nestjs/common';
import * as OTPAuth from 'otpauth';
import QRCode from 'qrcode';

const ISSUER = 'DashFlow';

@Injectable()
export class TwoFactorService {
  generateSecret(email: string): { secret: string; otpauthUri: string } {
    const secret = new OTPAuth.Secret({ size: 20 });
    const totp = new OTPAuth.TOTP({ issuer: ISSUER, label: email, secret });
    return { secret: secret.base32, otpauthUri: totp.toString() };
  }

  buildQrDataUrl(otpauthUri: string): Promise<string> {
    return QRCode.toDataURL(otpauthUri);
  }

  verify(secretBase32: string, code: string): boolean {
    const totp = new OTPAuth.TOTP({ issuer: ISSUER, secret: OTPAuth.Secret.fromBase32(secretBase32) });
    return totp.validate({ token: code, window: 1 }) !== null;
  }
}
```

- [ ] **Step 4:** Run → PASS. **Step 5: Commit** — `feat(b2): TwoFactorService (otpauth + qrcode)`

---

## Task 3: DTOs 2FA + login totpCode

**Files:** Modify `src/auth/dto/auth.dto.ts`

- [ ] **Step 1:** Ajouter/modifier dans `src/auth/dto/auth.dto.ts` :
```ts
export const totpVerifySchema = z.object({ code: z.string().length(6, 'Code à 6 chiffres requis') });
export const totpDisableSchema = z.object({ password: z.string().min(1, 'Mot de passe requis') });
export type TotpVerifyDto = z.infer<typeof totpVerifySchema>;
export type TotpDisableDto = z.infer<typeof totpDisableSchema>;
```
Et modifier `loginSchema` pour ajouter `totpCode` optionnel :
```ts
export const loginSchema = z.object({ email, password: z.string().min(1, 'Mot de passe requis'), totpCode: z.string().length(6).optional() });
```

- [ ] **Step 2:** `pnpm tsc --noEmit` clean. **Step 3: Commit** — `feat(b2): DTOs TOTP + totpCode optionnel au login`

---

## Task 4: AuthService — setup/enable/disable + gate login

**Files:** Modify `src/auth/auth.service.ts`; Test `src/auth/auth.service.spec.ts`

- [ ] **Step 1: Tests (TDD)** — ajouter à `src/auth/auth.service.spec.ts`. Le constructeur d'`AuthService` reçoit désormais `TwoFactorService` en 3e argument — mettre à jour le `beforeEach` : `svc = new AuthService(r as any, m as any, new TwoFactorService());`. Importer `TwoFactorService` et `OTPAuth`. Ajouter :
```ts
  it('enableTotp : code valide → totpEnabled set', async () => {
    const tf = new TwoFactorService();
    const { secret } = tf.generateSecret('a@b.com');
    svc = new AuthService(r as any, m as any, tf);
    r.findById.mockResolvedValue({ id: 'u1', totpSecret: secret, totpEnabled: null });
    r.updateUser.mockResolvedValue({ id: 'u1' });
    const totp = new OTPAuth.TOTP({ issuer: 'DashFlow', secret: OTPAuth.Secret.fromBase32(secret) });
    const res = await svc.enableTotp('u1', totp.generate());
    expect(res.success).toBe(true);
    expect(r.updateUser).toHaveBeenCalledWith('u1', expect.objectContaining({ totpEnabled: expect.any(Date) }));
  });

  it('enableTotp : code invalide → fail 400', async () => {
    const tf = new TwoFactorService();
    const { secret } = tf.generateSecret('a@b.com');
    svc = new AuthService(r as any, m as any, tf);
    r.findById.mockResolvedValue({ id: 'u1', totpSecret: secret, totpEnabled: null });
    expect((await svc.enableTotp('u1', '000000')).success).toBe(false);
  });

  it('login : compte 2FA sans code → fail 403 TOTP_REQUIRED', async () => {
    r.findByEmail.mockResolvedValue({ id: 'u1', email: 'a@b.com', password: await argon2.hash('bonmotdepasse'), emailVerified: new Date(), totpEnabled: new Date(), totpSecret: 'AAAA' });
    const res = await svc.login({ email: 'a@b.com', password: 'bonmotdepasse' });
    expect(res).toMatchObject({ success: false, status: 403, code: 'TOTP_REQUIRED' });
  });

  it('disableTotp : mauvais mot de passe → fail 401', async () => {
    r.findById.mockResolvedValue({ id: 'u1', password: await argon2.hash('bon-mdp-long-1'), totpSecret: 'X', totpEnabled: new Date() });
    expect((await svc.disableTotp('u1', 'mauvais')).status).toBe(401);
  });
```

- [ ] **Step 2:** Run → FAIL.

- [ ] **Step 3:** Modifier `src/auth/auth.service.ts` :
  - Injecter `TwoFactorService` : ajouter au constructeur `private readonly twoFactor: TwoFactorService,` (importer la classe).
  - Modifier `login` (juste avant le `return ok(user)` final) :
```ts
    if (user.totpEnabled && user.totpSecret) {
      if (!dto.totpCode) return fail(403, 'Code 2FA requis', 'TOTP_REQUIRED');
      if (!this.twoFactor.verify(user.totpSecret, dto.totpCode)) return fail(401, 'Code 2FA invalide');
    }
    return ok(user);
```
  - Ajouter les méthodes :
```ts
  async setupTotp(userId: string): Promise<Result<{ qrCode: string; secret: string; uri: string }>> {
    const user = await this.repo.findById(userId);
    if (!user) return fail(404, 'Compte introuvable');
    const { secret, otpauthUri } = this.twoFactor.generateSecret(user.email);
    await this.repo.updateUser(userId, { totpSecret: secret });
    const qrCode = await this.twoFactor.buildQrDataUrl(otpauthUri);
    return ok({ qrCode, secret, uri: otpauthUri });
  }

  async enableTotp(userId: string, code: string): Promise<Result<null>> {
    const user = await this.repo.findById(userId);
    if (!user || !user.totpSecret) return fail(400, 'Aucun secret 2FA en attente');
    if (!this.twoFactor.verify(user.totpSecret, code)) return fail(400, 'Code 2FA invalide');
    await this.repo.updateUser(userId, { totpEnabled: new Date() });
    return ok(null);
  }

  async disableTotp(userId: string, password: string): Promise<Result<null>> {
    const user = await this.repo.findById(userId);
    if (!user || !user.password) return fail(400, 'Aucun mot de passe défini');
    if (!(await argon2.verify(user.password, password))) return fail(401, 'Mot de passe incorrect');
    await this.repo.updateUser(userId, { totpSecret: null, totpEnabled: null });
    return ok(null);
  }
```
  (`LoginDto` inclut désormais `totpCode?` via la modif DTO de Task 3 ; importer `argon2` est déjà fait en B1.)

- [ ] **Step 4:** Run → PASS (anciens tests login B1 toujours verts — un user sans `totpEnabled` passe le gate). **Step 5: Commit** — `feat(b2): AuthService setup/enable/disable TOTP + gate login`

---

## Task 5: Controller endpoints + module

**Files:** Modify `src/auth/auth.controller.ts`, `src/auth/auth.module.ts`

- [ ] **Step 1:** Dans `src/auth/auth.controller.ts`, importer les DTO/schemas TOTP et ajouter (après les routes `/me/*` existantes) :
```ts
  @UseGuards(JwtAuthGuard, CsrfGuard) @Post('me/2fa/setup') @HttpCode(200)
  async totpSetup(@CurrentUser() u: AuthUser) {
    const r = await this.auth.setupTotp(u.id);
    if (!r.success) throw httpFrom(r);
    return r.data;
  }

  @UseGuards(JwtAuthGuard, CsrfGuard) @Post('me/2fa/verify') @HttpCode(200)
  async totpVerify(@CurrentUser() u: AuthUser, @Body(new ZodValidationPipe(totpVerifySchema)) dto: TotpVerifyDto) {
    const r = await this.auth.enableTotp(u.id, dto.code);
    if (!r.success) throw httpFrom(r);
    return { message: '2FA activée', totpEnabled: true };
  }

  @UseGuards(JwtAuthGuard, CsrfGuard) @Post('me/2fa/disable') @HttpCode(200)
  async totpDisable(@CurrentUser() u: AuthUser, @Body(new ZodValidationPipe(totpDisableSchema)) dto: TotpDisableDto) {
    const r = await this.auth.disableTotp(u.id, dto.password);
    if (!r.success) throw httpFrom(r);
    return { message: '2FA désactivée', totpEnabled: false };
  }
```
Ajouter aux imports du controller : `totpVerifySchema, totpDisableSchema` et types `TotpVerifyDto, TotpDisableDto` depuis `./dto/auth.dto`.

- [ ] **Step 2:** Dans `src/auth/auth.module.ts`, ajouter `TwoFactorService` aux `providers` (import en tête).

- [ ] **Step 3:** `pnpm test` (tous verts) + `pnpm tsc --noEmit` clean.

- [ ] **Step 4:** Boot + smoke : démarrer (`pnpm start:dev`, DB up), se connecter à un compte vérifié (register→verify pour récupérer le cookie + csrf), puis `POST /api/auth/me/2fa/setup` (avec cookie + header X-CSRF-Token) → 200 `{ qrCode, secret, uri }`. Arrêter le serveur.

- [ ] **Step 5: Commit** — `feat(b2): endpoints /me/2fa/setup|verify|disable + wiring`

---

## Task 6: e2e 2FA

**Files:** Modify `test/auth.e2e-spec.ts`

- [ ] **Step 1:** Ajouter un `it` au describe e2e (réutilise l'app + un nouvel email enrôlé). Le test : register→verify (cookie), récupère le CSRF via `/api/auth/csrf`, `setup` (récupère `secret`), calcule un code via `otpauth`, `verify` (active), `login` sans code → 403 `TOTP_REQUIRED`, `login` avec code calculé → 200.
```ts
import * as OTPAuth from 'otpauth';

it('2FA : setup → enable → login exige le code', async () => {
  const email2 = `e2e2fa+${Date.now()}@dashflow.test`;
  const server = app.getHttpServer();
  await request(server).post('/api/auth/register').send({ email: email2, password: 'motdepasse-long-12' }).expect(201);
  const verify = await request(server).post('/api/auth/verify').send({ email: email2, code: mailer.lastCode }).expect(200);
  const cookie = verify.headers['set-cookie'] as string | string[];
  const cookieArr = Array.isArray(cookie) ? cookie : [cookie];

  const csrf = await request(server).get('/api/auth/csrf').set('Cookie', cookieArr).expect(200);
  const csrfCookie = csrf.headers['set-cookie'] as string | string[];
  const allCookies = cookieArr.concat(Array.isArray(csrfCookie) ? csrfCookie : [csrfCookie]);
  const csrfToken = csrf.body.csrfToken as string;

  const setup = await request(server).post('/api/auth/me/2fa/setup')
    .set('Cookie', allCookies).set('X-CSRF-Token', csrfToken).expect(200);
  const secret = setup.body.secret as string;
  const totp = new OTPAuth.TOTP({ issuer: 'DashFlow', secret: OTPAuth.Secret.fromBase32(secret) });

  await request(server).post('/api/auth/me/2fa/verify')
    .set('Cookie', allCookies).set('X-CSRF-Token', csrfToken).send({ code: totp.generate() }).expect(200);

  await request(server).post('/api/auth/login').send({ email: email2, password: 'motdepasse-long-12' }).expect(403);
  await request(server).post('/api/auth/login')
    .send({ email: email2, password: 'motdepasse-long-12', totpCode: totp.generate() }).expect(200);
});
```

- [ ] **Step 2:** `pnpm test:e2e` → tous PASS (DB up). **Step 3: Commit** — `test(b2): e2e 2FA (setup→enable→login gate)`

---

## Self-Review

**Couverture du spec :**
- TwoFactorService (generate/qr/verify window 1) → Task 2 ✓
- setup/enable/disable → Task 4 ✓
- gate login 403 TOTP_REQUIRED / 401 invalide → Task 4 ✓
- Result.code + httpFrom → Task 1 ✓
- endpoints /me/2fa/* (auth+csrf) → Task 5 ✓
- DTOs + loginSchema totpCode → Task 3 ✓
- deps otpauth/qrcode → Task 1 ✓
- tests unit + e2e (critères 1-5) → Tasks 2,4,6 ✓
- secret base32 en clair → Task 4 (stocké tel quel) ✓

**Placeholders :** aucun. Code complet par étape.

**Cohérence des types :** `TwoFactorService.generateSecret`/`verify`/`buildQrDataUrl` (Task 2) utilisés Tasks 4,5,6. `fail(status,error,code?)` (Task 1) utilisé Task 4 (`TOTP_REQUIRED`). `loginSchema.totpCode` (Task 3) lu dans `login` (Task 4). `totpVerifySchema`/`totpDisableSchema` (Task 3) câblés Task 5. Constructeur `AuthService` à 3 args cohérent entre Task 4 (impl + specs) et le module (Task 5 ajoute le provider).
